---
title: Links
layout: layouts/base.njk
permalink: /links/
eleventyNavigation:
  key: Links
  order: 3
---
<ul class="pl-0 space-y-2 list-none">
  {% for item in links.items %}
    <li>
      <a href="{{ item.link }}" class="underline">
        {{ item.title }}<span class="inline-block ml-1 underline">↗</span>
      </a>
      <date class="italic text-gray-600" datetime="{{ item.date | htmlDateString }}">{{ item.date | readableDate }}</time>
      <p>{{ item.notes | safe }}</p>
    </li>
  {% endfor %}
</ul>
